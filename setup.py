import setuptools

setuptools.setup(
    name="UniteIO",
    version="0.1",
    author="dasgreb",
    description="Multi Async Infrastructure",
    packages=setuptools.find_packages(),
    include_package_data=True,
    python_requires='>=3.14.5',
    install_requires=[],
    entry_points={
        'console_scripts': [

        ]
    },
)
